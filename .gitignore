# folders
data
logs
archives
backups
prometheus

# files
.env

# branding — user-provided assets, only setup.sh is tracked
branding/*
!branding/setup.sh

# alerts — generated config contains credentials/thresholds, only templates/scripts tracked
alerts/config.yml
alerts.yml
alertmanager.yml